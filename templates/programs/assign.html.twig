{% extends 'base.html.twig' %}

{% block title %}Assign program{% endblock %}

{% block content %}

    <div class="container">

        <div class="row">
            <h1>Current program</h1>
        </div>
        <div class="row">
            <div class="col-4"> {{ program.name }}</div>
            <div class="col-4"> {{ program.description }}</div>
        </div>

        <div class="row">
            Assign device to:
        </div>

        <div class="row">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>

    </div>

{% endblock %}
